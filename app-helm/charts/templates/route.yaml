# App-helm-chart/charts/templates/route.yaml
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.Name }}-route
spec:
  to:
    kind: Service
    name: {{ .Values.Name }}-service
  port:
    targetPort: 80